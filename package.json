{
  "name": "mycelium-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "clean": "turbo run clean",
    "dev": "turbo run dev"
  },
  "devDependencies": {
    "turbo": "^2.5.6",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
